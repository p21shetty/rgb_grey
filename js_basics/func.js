// gree(name) is a parameter
function greet(name, lastName) {
    console.log('Hello'  +  name + ''  + lastName );
}
//calculating value
function square(number){
    return number * number
}

//let number = square(2);
console.log(square(2));
//greet('John' , 'Smith'); // argument of a function
